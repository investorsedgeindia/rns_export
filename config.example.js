/* ============================================
   NutLux — Runtime Configuration (TEMPLATE)
   Copy this file to `config.js` and fill in your real values.
   `config.js` is gitignored, so your secrets stay local.
   ============================================ */

window.SUPABASE_CONFIG = {
  url: 'https://YOUR-PROJECT-REF.supabase.co',
  anonKey: 'YOUR-PUBLISHABLE-ANON-KEY',
};